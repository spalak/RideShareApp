<%= form_for @ride_entry do |form| %>
	<%= form.label :location %>
	<%= form.text_field :location %>

	<%= form.label :start_date %>
	<%= form.date_field :start_date %>

	<%= form.label :start_time %>
	<%= form.time_field :start_time %>

	<%= form.submit "Save" %>
<% end %>
